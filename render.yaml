services:
  - type: web
    name: proxy-manager
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python proxy_manager.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
